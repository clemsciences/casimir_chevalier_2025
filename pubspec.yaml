name: casimir_chevalier_2025
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

  historical_timeline:
#    path: ../historical_timeline
    git:
      url: https://github.com/clemsciences/historical_timeline.git
      ref: master

  go_router:
  url_launcher: ^6.3.2
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/books/cloque/chapters/

